import { Center, Spinner } from '@chakra-ui/react'

const Loading = () => {
    return (
        <Center>
            <Spinner color='blue.500' width='10rem' height='10rem' />
        </Center>
    )
}
export default Loading
